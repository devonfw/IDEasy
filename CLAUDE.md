# CLAUDE.md

The shared agent instructions live in `AGENTS.md` (the single source of truth, also read natively
by GitHub Copilot). They are imported below so Claude Code picks them up automatically.

@AGENTS.md
