:toc:
toc::[]

= Artificial Intelligence

`IDEasy` supports to bring AI to your teams boosting their efficiency.
Mainly this is just done with generic features that can be also used for AI.
This guide gives some special hints to make your project and team `AI ready` with the help of IDEasy.

== AI Tools

IDEasy integrates many tools that you can install automatically including the CLI of famous AI tools like GitHub Copilot or Claude Code.
Therefore, simply install it via `ide install «tool» [«version»]` to your project.

To automatically install tools for all team members on link:project.adoc[project] creation, simply add them to the link:variables.adoc[variable] `IDE_TOOLS`.
For further details see also link:software.adoc[software].

[NOTE]
====
If an AI tool is not yet available in IDEasy, you can create a https://github.com/devonfw/IDEasy/issues/new?template=feature_request.yml[feature-request].
====

== AI Plugins

IDEasy can automatically install arbitrary plugins into the IDE of your choice (IntelliJ, Eclipse, VSCode, etc.) as described in the link:plugin.adoc[plugin] section.

Typically, developers want to have the AI integrated into their IDE.
Therefore, we already preconfigured and tagged common AI plugins for various IDEs.
To give an example, let's assume you are using IntelliJ and you want to use GitHub Copilot you can find the plugin configuration https://github.com/devonfw/ide-settings/blob/main/intellij/plugins/GitHubCopilot.properties[here].
In your forked settings ensure that all your desired plugins are activated via

```
active=true
```

[NOTE]
====
If you are using older settings, the property may still be named `plugin_active`.
This is a legacy name for compatibility with older __devonfw-ide __versions.
====

== AI Config

Your AI tools shall be configured and tailored to the needs of your project.
This includes aspects like instructions, skills, hooks, etc.
However, common AI tools expect them in configuration folders directly in your workspace top-level directory.
Even worse there is no common standard yet and every AI tool has its own naming conventions.
To avoid anti-patterns and still give you high flexibility and usability, we created a special feature to automatically create additional symbolic links after cloning of a link:repository.adoc[repository].

The following repository configuration in `$IDE_HOME/settings/repositories/ai.properties` illustrates the idea as example:

```
path=ai
git_url=https://github.com/devonfw/example-ai-repo.git
workspaces=*
link=.claude,.github,.kiro
active=true
tags=ai
```

This will clone the specified git repository into the sub-folder `ai` of your workspace `main` and also link it to every additional workspace you have (`workspaces=*`).
Further it will create the specified symbolic links in your workspace pointing to the `ai` repository.

This will result in the following structure in each of your workspaces:

```
/
├──/ .claude/ → ai/
├──/ .github/ → ai/
├──/ .kiro/ → ai/
├──/ ai/
│  ├──/ hooks/
│  ├──/ rules/
│  ├──/ skills/
│  ├──/ steering/
│  ├──/ CLAUDE.md
│  └──/ copilot-instructions.md
└──/ .../
```

This is just an example.
Typically, you will only need support for a single type of AI.
If your repository already contains tool-specific folders (e.g. `.github`` for Copilot), simply configure something like this:

```
link=.github=.github
```

This will create a symbolic link called `.github` pointing to the `.github` folder inside your git repository (e.g. `ai/.github`).

However, you can also map specific subfolders:

```
.github/hooks=myhooks/github
```

So if your git repo would contain the hooks for GitHub Copilot in the folder `myhooks/github` those will be linked to appear as `.github/hooks` in your workspace.
We hope this gives you ultimate flexibility to solve all problems you may have.

== Isolated configuration

IDEasy isolates tools and configuration in a link:sandbox.adoc[sandbox].
Therefore, different IDEasy projects on the same computer may use the same tool in different version or with different configuration.
In case you work for multiple projects (and customers) on the same machine, you want to isolate the project environments.
This also applies to AI tooling where maybe one of your customers may pay a subscription for an AI tool and you only want to use it for that specific project.
Assuming another project may be hightly restricted and confidential then you also do not want to accidentally send the project code to some AI cloud service of a different project.

Example for Claude CLI: `ide claude` (or simply `claude` while an IDEasy project is active) points `CLAUDE_CONFIG_DIR` at `$IDE_HOME/conf/claude` and seeds a user-owned `$IDE_HOME/conf/claude/settings.json` skeleton on first install.

On top of relocating the config folder, IDEasy scrubs provider/auth environment variables so a value leaked into your shell cannot silently override this project's configuration.
You need to understand this behavior in order to debug, circumvent or influence it:

* Before launching Claude, IDEasy removes the following variables *when they are only inherited from your ambient shell/system environment*: `ANTHROPIC_*`, `CLAUDE_CODE_USE_BEDROCK`/`VERTEX`/`FOUNDRY`, `CLAUDE_CODE_OAUTH_TOKEN`, `AWS_PROFILE`, `AWS_REGION`, `AWS_*_KEY*`, `AWS_SESSION_TOKEN`, `AWS_BEARER_TOKEN_BEDROCK`.
* A value you declare *intentionally* in an IDEasy `ide.properties` (e.g. `export ANTHROPIC_MODEL=...` or `export BEDROCK_MODEL_ID=...` in `settings/ide.properties` to share it across your team) is *not* scrubbed and takes precedence over the ambient value.
* Put per-user provider/auth secrets in `conf/claude/settings.json` under the `env` block (see the generated `conf/claude/README.md` for Bedrock and custom-endpoint examples), since ambient values for those variables are removed.

[NOTE]
====
Known limitations: on macOS, Claude.ai _subscription_ login credentials are stored in the system Keychain, which `CLAUDE_CONFIG_DIR` does not relocate (use Bedrock/API-token auth to isolate on macOS; not relevant for Bedrock setups).
An enterprise `managed-settings.json` overrides all projects by design.
A bare `claude` run while no IDEasy project is active is not scrubbed - keep provider credentials out of your shell rc files.
====
